import { Component } from "react";
import ARScene from "./ARScene"

const Main: React.FC = ({}) => {
    return (
        <div className="App">
            <ARScene />
        </div>
    );
};

export default Main;